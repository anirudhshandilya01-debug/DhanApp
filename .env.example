# Optional. Dhan Advisor works out of the box using public RSS feeds from
# whitelisted, reputable Indian financial publishers (no key required).
#
# If you later wire in a paid market-data API for live prices, put the key here.
# The app reads this at runtime; it is never committed (see .gitignore).
MARKET_DATA_API_KEY=
