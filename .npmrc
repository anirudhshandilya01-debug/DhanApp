# Use the official Electron release mirror. If you are behind a proxy or the
# default download fails, you can point this at a local mirror instead.
electron_mirror=https://github.com/electron/electron/releases/download/
